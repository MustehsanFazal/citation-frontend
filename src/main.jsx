import React from "react"
import ReactDOM from "react-dom/client"
import CitationGenerator from "./App"

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CitationGenerator />
  </React.StrictMode>
)
